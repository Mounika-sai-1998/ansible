- name: variables from prompt
  hosts: localhost
  vars_prompt:
    - name: USERNAME
      prompt: please enter your USERNAME
      private: false
    - name: PASSWORD
      prompt: please enter your password
      private: yes
  tasks:
    - name: print info of a variables
      ansible.builtin.debug:
        msg: "username : {{USERNAME}, password : {{PASSWORD}}"